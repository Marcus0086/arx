/target
.cache
.cargo-home
.direnv
result
# Ignore test artifacts but NOT the tests/ source directories
test_*
!tests/
!**/tests/
*.bin
dataset
.DS_Store
*.arx
*.delta
*.log

# Docker — real tenant keys (example file is tracked, populated config is not)
config/tenants.json
.env
MONKY-V0